# 電子紙專案交接紀錄

最後更新：2026-09-21（Asia/Taipei）

## 一、目前專案狀態

- ESP32 已成功上傳 MQTT 版本。
- 最近一次上傳埠：`COM4`
- 最近一次上傳程式：`45_epaper_mqtt`
- 程式檔案：
  - `examples/45_epaper_mqtt/45_epaper_mqtt.ino`
  - `examples/00_epaper_mqtt/00_epaper_mqtt.ino`（早期存檔，保留作為歷史版本）
  - `examples/00_epaper_dht/00_epaper_dht.ino`（未加入 MQTT 的感測顯示版本）
- 板型：Arduino CLI `esp32:esp32:esp32wrover`
- 不使用 Arduino IDE，使用 Arduino CLI 編譯與上傳。

## 二、電子紙硬體

- 品牌：微雪 Waveshare
- 尺寸：2.9 吋
- 解析度：`296 x 128`
- 類型：三色電子紙
- 模組：V4
- 驅動板：Rev2.1
- GxEPD2 顯示類別：`GxEPD2_290_C90c`
- 顯示方向：Rotation 1
- 顯示更新週期：每 60 秒
- 顯示內容：
  - TEMP：溫度、數值、`°C`
  - HUM：濕度、數值、`%RH`
  - LIGHT：亮度、數值、`%`
  - 感測器讀取無效時顯示 `ERR`

### 電子紙接線

| 電子紙腳位 | ESP32 GPIO |
|---|---:|
| DIN / MOSI | GPIO23 |
| SCK | GPIO18 |
| CS | GPIO27 |
| DC | GPIO26 |
| RST | GPIO25 |
| BUSY | GPIO34 |

## 三、感測器與燈號

| 裝置 | ESP32 GPIO | 用途 |
|---|---:|---|
| DHT11 | GPIO14 | 溫度、濕度 |
| 光敏電阻 | GPIO33 | 類比亮度，換算為 0～100% |
| 燈號 | GPIO2 | MQTT 控制輸出，預設高電位有效 |

注意：GPIO14 已給 DHT11、GPIO33 已給光敏電阻，不可再拿來當燈號輸出。

## 四、函式庫

專案或 Arduino 函式庫中已使用：

- `GxEPD2`
- `Adafruit GFX Library`
- `Adafruit BusIO`
- `SimpleDHT`
- `PubSubClient 2.8.0`

## 五、Wi‑Fi 設定

- SSID：`Galaxy S24+ 29EB`
- 密碼：已寫入 `45_epaper_mqtt.ino` 的 `WIFI_PASSWORD`
- Wi‑Fi 模式：`WIFI_STA`
- 啟用自動重連
- 連線重試間隔：15 秒

為避免在交接文件中重複暴露 Wi‑Fi 密碼，密碼只保留在程式設定區；若更換手機熱點，需同步修改 `WIFI_SSID` 與 `WIFI_PASSWORD`。

## 六、MQTT 設定

- Broker：`MQTTGO.io`
- Port：`1883`
- Client ID：`MQTTGO-6992413901`
- MQTT 帳號：空白，使用匿名連線
- MQTT 密碼：空白
- MQTT 重試間隔：5 秒
- 使用函式庫：`PubSubClient`

### MQTT Topic

| 功能 | Topic | 方向 |
|---|---|---|
| 溫度 | `epaper/temperature` | ESP32 發布 |
| 濕度 | `epaper/humidity` | ESP32 發布 |
| 亮度 | `epaper/light` | ESP32 發布 |
| 燈號控制 | `epaper/lamp/set` | ESP32 訂閱 |
| 燈號狀態 | `epaper/lamp/state` | ESP32 發布 |

溫度、濕度與亮度每次感測後以 retain 訊息發布；感測失敗時發布 `ERR`。

### 燈號控制訊息

發布至 `epaper/lamp/set`：

- `ON`、`1`、`TRUE`：GPIO2 開啟
- `OFF`、`0`、`FALSE`：GPIO2 關閉

目前燈號狀態會 retain 發布至 `epaper/lamp/state`。

## 七、流水號存檔規則

檔名開頭的數字是存檔流水號，不是固定文字 `00`。

- `00_XXXYYZZ` 是檔名格式示意，不代表每次都要使用 `00`。
- 目前已建立：
  - `00_epaper_dht`
  - `00_epaper_mqtt`
  - `45_epaper_mqtt`
- 目前使用到流水號 `45`。
- 下一個新版本應使用 `46_` 開頭，例如：
  - `46_epaper_mqtt`
  - `46_epaper_dht`
- 後續依序使用 `47_`、`48_`……，不可回頭把新版本命名成 `00_`。
- 建立新檔前，先確認該流水號是否已存在，避免覆蓋既有版本。

## 八、編譯與上傳

Arduino CLI 路徑：

```text
C:\Users\user\AppData\Local\Programs\Arduino IDE\resources\app\lib\backend\resources\arduino-cli.exe
```

板型：

```text
esp32:esp32:esp32wrover
```

上傳埠：

```text
COM4
```

若中文路徑造成 linker 或編譯問題，先將草稿複製到純英文暫存路徑，例如：

```text
C:\epaper_mqtt_stage_20260921_v2
```

再使用 Arduino CLI 編譯與上傳。

## 九、下一次交接注意事項

1. 先確認 ESP32 是否出現在 `COM4`。
2. 先確認新版本流水號；目前下一號是 `46`。
3. 變更 Wi‑Fi、MQTT Broker、Topic 或燈號 GPIO 後，需同步更新本文件。
4. 不可將 GPIO14 或 GPIO33 改作燈號輸出。
5. 若 MQTTGO 改為需要帳號密碼，需填入 `MQTT_USER` 與 `MQTT_PASSWORD`，並重新編譯上傳。
